#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints the alphabets from a-z
 *
 * Return: Always 0 (success)
*/
int main(void)
{
	for (char c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
		putchar('\n');

		return (0);
}
